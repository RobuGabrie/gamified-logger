# Gogosica Quest

A mobile-first, gamified PWA built with Next.js and Tailwind CSS.

## Run locally

- Install dependencies: `npm install`
- Start the dev server: `npm run dev`

## PWA

The app registers a basic service worker for offline shell caching and includes a web manifest for add-to-home-screen on iPhone.
